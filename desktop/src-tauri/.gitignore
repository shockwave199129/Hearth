/target
# Frozen backend + bundled llama-server — build artifacts, regenerated by
# scripts/build_backend.sh/.ps1 and scripts/fetch_llama_cpp.py (see
# .github/workflows/build.yml and README.md).
/resources
# ACL/capability schemas — regenerated by every `cargo build`/`tauri dev`.
/gen
