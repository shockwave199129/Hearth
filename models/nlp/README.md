# Hearth NLP ONNX package
#
# Regenerated by:
#   cd hearth_ai && PYTHONPATH=. python3 examples/export_onnx.py --smoke
#
# Layout:
#   tokenizer.json          shared BPE tokenizer
#   manifest.json           parity report + config
#   encoder/model.onnx      pooled encoder output
#   {emotion,intent,memory,relationship,strategy}/
#     model.onnx + config.json + labels.json
#
# Runtime: set NLP_MODELS_DIR to this directory (see plan setup-eval todo).
