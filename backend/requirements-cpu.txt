# Tier B/C — Kokoro-82M TTS fallback (tts/tts_engines.py's
# KokoroEngine). See requirements-common.txt for why this is split from
# requirements-gpu.txt at all.
-r requirements-common.txt

# NeuML/kokoro-fp16-onnx (huggingface.co/NeuML/kokoro-fp16-onnx), used
# directly via onnxruntime + ttstokenizer per that model card's ONNX
# Runtime usage example — not its txtai pipeline alternative, whose base
# install pulls in torch/transformers/faiss-cpu regardless (deliberately
# NOT used here, to keep this tier's footprint the lightweight
# onnxruntime-only one tier B/C's low-resource machines actually need).
onnxruntime>=1.20.1
ttstokenizer>=1.1.0
