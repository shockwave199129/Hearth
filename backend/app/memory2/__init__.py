"""Book Volume 4 — the episodic/semantic memory system.

Distinct from the legacy flat `app.memory.long_term` module (kept as-is,
still backing the existing tool-based `/api/memories` surface). This module
is the versioned, tiered replacement described in Volume 4: EpisodicMemory /
SemanticMemory / EmotionalMetadata models, rule-based formation, clustering-
based promotion/consolidation, arithmetic decay, deterministic retrieval, and
rule-based contradiction handling — written to exclusively by the Growth
Engine (`app.growth.engine`)."""
