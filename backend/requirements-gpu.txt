# Tier S/A — Parler-TTS-Tiny-v1 (tts/tts_engines.py's ParlerEngine). See
# requirements-common.txt for why this is split from requirements-cpu.txt
# at all.
-r requirements-common.txt

# Installed from source (HF's own quickstart recommends this over the
# 0.2.3 PyPI release) rather than a version pin — no wheel is published for
# a git ref, so this line has no --only-binary=:all: candidate; see
# build_backend.sh/.ps1, which drop that flag specifically for this file's
# install step (it still applies to requirements-common.txt above).
parler-tts @ git+https://github.com/huggingface/parler-tts.git
