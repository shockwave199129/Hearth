# Tier S/A — Parler-TTS-Tiny-v1 (tts/tts_engines.py's ParlerEngine). See
# requirements-common.txt for why this is split from requirements-cpu.txt
# at all.
-r requirements-common.txt

# Explicit pin for first-run `pip install --target` (app/setup/orchestrator.py
# skips `-r` includes because common is already frozen at CI). Without this
# line in the GPU install set, parler-tts -> descript-audiotools -> librosa
# -> numba resolves an unbounded numpy, backtracks numba to 0.53.1, and
# pulls llvmlite 0.36 which only supports Python<3.10 — breaking the
# bundled setup Python 3.12. Same ceiling as requirements-common.txt
# (numba 0.66.0's own `numpy<2.5`). Install numpy BEFORE parler-tts.
numpy>=2.0.0,<2.5

git+https://github.com/huggingface/parler-tts.git
